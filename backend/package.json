{
  "name": "premier-erp-backend",
  "version": "2.0.0",
  "description": "Premier ERP backend — shares the ISO/LMS MySQL database (ptis_erp_db) for logins, employees, departments and locations. Runs as its own process on its own port; never modifies iso-server-backend-PTIS.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "migrate": "node migrate.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "mysql2": "^3.0.0"
  },
  "license": "UNLICENSED",
  "private": true
}
